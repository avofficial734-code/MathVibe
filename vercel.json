{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "composer install --no-dev --optimize-autoloader && npm ci && npm run build && php artisan config:cache && php artisan route:cache && php artisan view:cache",
  "outputDirectory": "public",
  "installCommand": "yum install -y php php-cli php-mbstring php-xml php-curl php-zip unzip || true",
  "functions": {
    "api/lambda.php": {
      "runtime": "vercel-php@0.7.4",
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/api/lambda.php"
    }
  ]
}
